
void drawSky() {
  background(134,153,255);
  
  // sky (change color based on time of day)
  /*if (hour() < 12) {
    float r = map(hour(), 0, 12, 6, 134);
    float g = map(hour(), 0, 12, 15, 153);
    float b = map(hour(), 0, 12, 64, 255);
    background(r, g, b);
  }
  else {
    float r = map(hour(), 12, 23, 6, 134);
    float g = map(hour(), 12, 23, 15, 153);
    float b = map(hour(), 12, 23, 64, 255);
    background(r, g, b);
  }*/
}
